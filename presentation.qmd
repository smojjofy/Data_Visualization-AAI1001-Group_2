---
title: "Southeast Asia Energy Consumption Visualization Project"
author: "Group 2"
date: "2026-07-13"
format:
  revealjs:
    theme: [default]
    slide-number: true
    incremental: false
    embed-resources: true
---

## Team Introduction

```{r}
#| include: false
# Global setup chunk for libraries, options, and shared objects.

library(ggplot2)
library(dplyr)
library(readr)
library(stringr)

library(tidyr)
library(forcats)

data_chart_text_size <- 16
data_chart_axis_text_size <- 17
data_chart_legend_text_size <- 16
data_chart_geom_text_size <- 4
data_chart_theme <- function(legend_position = "right", axis_x_angle = 0, axis_x_hjust = 1) {
  ggplot2::theme_minimal(base_size = data_chart_text_size) +
    ggplot2::theme(
      plot.title = ggplot2::element_text(size = data_chart_text_size + 2, face = "bold"),
      plot.subtitle = ggplot2::element_text(size = data_chart_text_size),
      axis.title = ggplot2::element_text(size = data_chart_axis_text_size),
      axis.text = ggplot2::element_text(size = data_chart_axis_text_size - 1),
      axis.text.x = ggplot2::element_text(angle = axis_x_angle, hjust = axis_x_hjust, size = data_chart_axis_text_size - 1),
      legend.title = ggplot2::element_text(size = data_chart_legend_text_size),
      legend.text = ggplot2::element_text(size = data_chart_legend_text_size),
      strip.text = ggplot2::element_text(size = data_chart_axis_text_size - 1, face = "bold"),
      legend.position = legend_position
    )
}
```

```{=html}
<style>
  :root {
    --data-chart-text-size: 90%;
  }
  .data-chart-note {
    font-size: var(--data-chart-text-size);
  }
  .thank-you-center {
    display: flex;
    align-items: center;
    justify-content: center;
    height: 80vh;
    width: 100%;
  }
  .thank-you-center h1 {
    margin: 0;
    text-align: center;
  }
  section.thank-you > h2 {
    display: none;
  }
  section.thank-you {
    padding-top: 0;
  }
  .final-chart-columns {
    align-items: flex-start;
  }
  .final-chart-columns .column {
    padding-top: 0;
  }
  .final-chart-note {
    margin-top: 0.15rem;
    line-height: 1.35;
    padding-right: 0.35rem;
  }
  .final-chart-note ul {
    margin-top: 0.35rem;
    margin-bottom: 0.25rem;
    padding-left: 1rem;
  }
  .final-chart-note li + li {
    margin-top: 0.2rem;
  }
  .final-chart-note.final-left {
    font-size: 95%;
  }
  .final-chart-note.final-left ul {
    margin-top: 0.5rem;
  }

</style>
```
Group 2 Members:
Lim Ze Kai
Ziven Wang
Tan Jing Yu
Ivan Tan
Jaison Tan

## Project Overview
After initial general research across ASEAN countries, we narrowed down on energy visualizations as they a often poorly represented.

Our presentation documents a critique and improvement of an existing visualization of Southeast Asia's energy consumption.

## Project Objectives

**First**, how does fuel consumption and their type vary across the different ASEAN nations?

**Second**, are there meaningful structural differences in how energy is used across sectors that aggregate regional narratives conceal?

**Third**, does the relationship between a country's primary fuel source and its dominant end-use sector support or challenge the narrative that developing, agrarian-industrial economies face fundamentally different inflationary energy risks than hyper-dense, import-dependent commercial hubs?

## Data Context {.smaller}

- Source dataset: `data/energy_data.csv`
- Scope: Southeast Asia, 2024
- Structure: long-format data with country, fuel type, end-use sector, percentage, and panel fields
- **Important note: due to data quality issues at the source (e.g. inconsistent country naming conventions such as "Lao DPR" and "Viet Nam"), the original data could not be used as-is. We therefore built a structurally faithful synthetic proxy dataset that mirrors the real structure and distributions for this project.**

## Existing Chart {.smaller}

::: {.columns}

::: {.column width="25%"}

<div class="data-chart-note">

- Dual-panel 100% stacked bars: fuel mix and end-use sector mix across ASEAN.
- Values are normalized to 100% for each country and panel.

</div>

:::

::: {.column width="75%"}

![Original IEA chart - "Total Final Consumption mix by fuel and end-use sector in Southeast Asia, 2024"](images/total-final-consumption-mix-by-fuel-and-end-use-sector-in-southeast-asia-2024.png){width="100%" fig-align="center"}

:::

:::

## Existing Chart: Strengths & Weaknesses {.smaller}

::: {.columns}

::: {.column width="48%"}

### Strengths

- Compact regional overview.
- Dual-panel layout links fuel and sector composition.
- Part-to-whole scale supports comparative reading.

:::

::: {.column width="48%"}

### Weaknesses

- No on-chart legend, so colors cannot be decoded without source metadata.
- Two category systems (fuel and sector) raise cognitive load.
- Percentage-only values hide absolute energy scale differences.
- Mid-stack segment comparisons are hard.
- Heavy color dependence reduces accessibility.

:::

:::

## Critical Analysis - What Works

- **S1:** Compact regional overview across all ten ASEAN countries
- **S2:** Useful dual-panel structure linking fuel mix and end-use sector mix
- **S3:** 100% stacked format supports proportional comparison

**Key message:** the original chart is useful as a broad regional overview, but it has limitations for detailed comparison.

## Critical Analysis - Key Weaknesses

<table>
  <colgroup>
    <col style="width: 22%;">
    <col style="width: 78%;">
  </colgroup>
  <thead>
    <tr>
      <th>Weakness</th>
      <th>How it affects interpretation</th>
    </tr>
  </thead>
  <tbody>
    <tr><td>W1-W2</td><td>Two category systems increase mental switching and reduce segment-level comparability.</td></tr>
    <tr><td>W3</td><td>Percentage-only values hide total energy scale and may overstate small countries.</td></tr>
    <tr><td>W4</td><td>Color decoding dominates, which hurts accessibility and speed.</td></tr>
    <tr><td>W5-W6</td><td>Weak ordering and annotation make targeted country comparisons harder.</td></tr>
  </tbody>
</table>

## Proposed Improvements

| Weakness | Improvement |
|---|---|
| W1 | Clearer separation of fuel and sector panels |
| W2 | Horizontal layout or aligned comparison design |
| W3 | Data note explaining the percentage-only limitation |
| W4 | Colourblind-safe palette and cleaner legend |
| W5 | Selective annotations and callouts |
| W6 | Sort countries by a meaningful analytical metric |

## Redesign Direction

<div style="font-size: 90%;">

- Keep the original chart's regional comparison purpose.
- Preserve the dual analytical lens: fuel mix and end-use sector.
- Improve readability using aligned panels or a horizontal structure.
- Improve accessibility through better colour and labelling.
- Improve storytelling using annotations and meaningful ordering.
- Include a caption noting that the chart is based on a structurally faithful synthetic proxy of the original IEA visualization.

</div>

## Analysis Plan

<div style="font-size: 87%;">

- Summarize the fuel mix and end-use sector mix for each ASEAN country using the percentage shares in the dataset, and identify the dominant categories in each case.
- Compare countries by fossil-fuel reliance, low-carbon share, and sector concentration in industry, transport, and buildings, while validating that each country-dimension pair sums to 100%.
- Identify each country's dominant fuel and dominant sector, then derive a single overall vulnerability ranking based on the overlap of those dominant shares to compare relative exposure to energy-demand pressure.

</div>

## Reproducible Workflow {.smaller}

::: {.columns}

::: {.column width="42%"}

- Load from `data/energy_data.csv` using a project-relative path for portability.
- Normalize country names, coerce numeric percentages, and set factor levels for stable ordering.
- Derive indicator-ready metrics (for example, fossil share and industry/consumer-demand shares).
- Generate plots from this single cleaned object, then render directly to `presentation.qmd`.

:::

::: {.column width="58%"}

```{r}
#| label: workflow-prepare
#| echo: true
#| eval: true
#| message: false
#| warning: false
#| results: "hide"
country_levels <- c(
  "Brunei Darussalam", "Cambodia", "Indonesia", "Lao PDR",
  "Malaysia", "Myanmar", "Philippines", "Singapore", "Thailand", "Vietnam"
)

normalize_country_name <- function(country) {
  dplyr::recode(
    stringr::str_squish(country),
    "Lao DPR" = "Lao PDR",
    "Viet Nam" = "Vietnam",
    .default = stringr::str_squish(country)
  )
}

energy_data <- readr::read_csv(
  file.path("data", "energy_data.csv"),
  show_col_types = FALSE
) |>
  dplyr::mutate(
    Country = factor(normalize_country_name(Country), levels = country_levels),
    Percentage = as.numeric(Percentage),
    Dimension = factor(stringr::str_squish(Dimension), levels = c("Fuel", "Sector")),
    Category = factor(stringr::str_squish(Category))
  )

energy_summary <- energy_data |>
  dplyr::mutate(
    fuel_fossil = ifelse(Dimension == "Fuel" & Category %in% c("Oil", "Coal", "Natural Gas"), Percentage, 0),
    sector_consumer = ifelse(Dimension == "Sector" & Category %in% c("Transport", "Buildings"), Percentage, 0)
  )
```

:::

:::

## Reproducibility {.smaller}

::: {.columns}

::: {.column width="42%"}

- Checks are coded to fail fast: missing values, bounds, duplicate rows, and composition sums.
- Each `(Country, Dimension)` pair must sum to `100 +/- 0.01`.
- Render commands are fixed: `quarto preview presentation.qmd` and `quarto render presentation.qmd` from repo root.

:::

::: {.column width="58%"}

```{r}
#| label: workflow-checks
#| echo: true
#| eval: true
#| message: false
#| warning: false
#| results: "hide"
required_cols <- c("Country", "Dimension", "Category", "Percentage")
stopifnot(all(required_cols %in% names(energy_data)))
stopifnot(all(!is.na(energy_data$Country)), all(!is.na(energy_data$Dimension)), all(!is.na(energy_data$Category)), all(!is.na(energy_data$Percentage)))
stopifnot(all(energy_data$Percentage >= 0), all(energy_data$Percentage <= 100))
stopifnot(nrow(energy_data |> dplyr::count(Country, Dimension, Category) |> dplyr::filter(n > 1)) == 0)

composition_check <- energy_data |>
  dplyr::group_by(Country, Dimension) |>
  dplyr::summarise(total_pct = sum(Percentage, na.rm = TRUE), .groups = "drop")

stopifnot(all(abs(composition_check$total_pct - 100) <= 1e-2))
sessionInfo()
```

:::

:::

## Final Data Visualization {.smaller}

::: {.columns .final-chart-columns}

::: {.column width="30%"}

<div class="data-chart-note final-chart-note">

- Dual-panel 100% stacked bars for fuel mix and sector mix across ASEAN.
- Country ordering is fixed for fair cross-country comparison.
- Values are normalized by panel and reflect extracted percentage shares.

</div>

:::

::: {.column width="70%"}

```{r}
#| label: load-energy-data
#| eval: true
#| echo: false
#| message: false
#| warning: false

country_levels <- c(
  "Brunei Darussalam", "Cambodia", "Indonesia", "Lao PDR",
  "Malaysia", "Myanmar", "Philippines", "Singapore", "Thailand", "Vietnam"
)

normalize_country_name <- function(country) {
  dplyr::recode(
    stringr::str_squish(country),
    "Lao DPR" = "Lao PDR",
    "Viet Nam" = "Vietnam",
    .default = stringr::str_squish(country)
  )
}

load_energy_data <- function(filepath = file.path("data", "energy_data.csv")) {
  d <- readr::read_csv(filepath, show_col_types = FALSE)
  dplyr::mutate(
    d,
    dplyr::across(where(is.character), stringr::str_squish),
    Country = factor(normalize_country_name(Country), levels = country_levels),
    Dimension = factor(Dimension, levels = c("Fuel", "Sector")),
    Category = factor(Category),
    Percentage = as.numeric(Percentage)
  )
}

if (!exists("energy_data")) {
  energy_data <- load_energy_data()
}

energy_data <- energy_data |>
  dplyr::mutate(
    dplyr::across(where(is.character), stringr::str_squish),
    Country = factor(normalize_country_name(Country), levels = country_levels),
    Dimension = factor(
      stringr::str_squish(as.character(Dimension)),
      levels = c("Fuel", "Sector")
    ),
    Category = factor(stringr::str_squish(as.character(Category))),
    Percentage = as.numeric(Percentage)
  ) |>
  dplyr::arrange(Dimension, Country, Category)
```

```{r}
#| label: validate-aggregate-energy-data
#| eval: true
#| echo: false
#| message: false
#| warning: false

validation_tbl <- energy_data |>
  dplyr::group_by(Country, Dimension) |>
  dplyr::summarise(
    Total_Percentage = sum(Percentage, na.rm = TRUE),
    .groups = "drop"
  )

country_metrics <- energy_data |>
  dplyr::group_by(Country) |>
  dplyr::summarise(
    fossil_fuel_share = sum(Percentage[Dimension == "Fuel" & Category %in% c("Oil", "Coal", "Natural Gas")], na.rm = TRUE),
    electricity_share = sum(Percentage[Dimension == "Fuel" & Category == "Electricity"], na.rm = TRUE),
    industry_share = sum(Percentage[Dimension == "Sector" & Category == "Industry"], na.rm = TRUE),
    .groups = "drop"
  )

stopifnot(all(abs(validation_tbl$Total_Percentage - 100) < 1e-6))
stopifnot(all(country_metrics$Country %in% energy_data$Country))
```

```{r}
#| label: plot-energy-composition
#| echo: false
#| eval: true
#| fig-width: 12
#| fig-height: 10
#| out-width: "100%"
#| fig-align: center
#| dev: png
#| message: false
#| warning: false

plot_data <- energy_data |>
  dplyr::arrange(Dimension, Country, Category) |>
  dplyr::mutate(
    Country = factor(as.character(Country), levels = country_levels)
  )

fuel_colors <- c(
  "Oil" = "#d95f02",
  "Coal" = "#7570b3",
  "Natural Gas" = "#1b9e77",
  "Bioenergy" = "#66a61e",
  "Electricity" = "#e7298a",
  "Other Fuels" = "#666666"
)

sector_colors <- c(
  "Industry" = "#1f78b4",
  "Transport" = "#e6ab02",
  "Buildings" = "#66c2a5",
  "Other" = "#8da0cb"
)

plot_colors <- c(fuel_colors, sector_colors)

ggplot2::ggplot(
  plot_data,
  ggplot2::aes(x = Percentage, y = Country, fill = Category)
) +
  ggplot2::geom_col(width = 0.8, colour = "white", linewidth = 0.2) +
  ggplot2::facet_wrap(~ Dimension, ncol = 2) +
  ggplot2::scale_x_continuous(
    limits = c(0, 100),
    labels = function(x) paste0(x, "%")
  ) +
  ggplot2::scale_fill_manual(values = plot_colors) +
  ggplot2::labs(
    title = "Southeast Asia final energy consumption mix by country",
    subtitle = "Fuel and sector composition, 2024",
    x = "Share of final energy consumption (%)",
    y = NULL,
    fill = "Category",
    caption = "Data note: Exact percentage shares extracted from IEA interactive chart tooltips (2024)."
  ) +
  data_chart_theme(legend_position = "bottom") +
  ggplot2::theme(
    panel.grid.major.y = ggplot2::element_blank(),
    panel.grid.minor = ggplot2::element_blank(),
    plot.margin = ggplot2::margin(5, 5, 5, 5)
  )
```

:::

:::

## Final Data Analysis {.smaller}

::: {.columns .final-chart-columns}

::: {.column width="30%"}

<div class="data-chart-note final-chart-note">

- Ranks countries by how vulnerable each country is across its leading fuel and leading sector.
- Bar fill is **Dominant Fuel** and text labels indicate the **Dominant Sector**.

</div>

:::

::: {.column width="70%"}

```{r}
#| label: final-analysis-metrics
#| eval: true
#| echo: false
#| message: false
#| warning: false

analysis_country_levels <- c(
  "Brunei Darussalam", "Cambodia", "Indonesia", "Lao PDR",
  "Malaysia", "Myanmar", "Philippines", "Singapore", "Thailand", "Vietnam"
)

analysis_data <- energy_data |>
  dplyr::mutate(
    Country = dplyr::coalesce(
      as.character(normalize_country_name(as.character(Country))),
      as.character(Country)
    ),
    Dimension = stringr::str_squish(as.character(Dimension)),
    Category = stringr::str_squish(as.character(Category)),
    Percentage = as.numeric(Percentage)
  ) |>
  dplyr::filter(!is.na(Country), !is.na(Dimension), !is.na(Category))

fuel_top <- analysis_data |>
  dplyr::filter(Dimension == "Fuel") |>
  dplyr::group_by(Country, Category) |>
  dplyr::summarise(share = sum(Percentage, na.rm = TRUE), .groups = "drop") |>
  dplyr::group_by(Country) |>
  dplyr::slice_max(order_by = share, n = 1, with_ties = FALSE) |>
  dplyr::transmute(
    Country = as.character(Country),
    dominant_fuel = as.character(Category),
    dominant_fuel_share = share
  )

sector_top <- analysis_data |>
  dplyr::filter(Dimension == "Sector") |>
  dplyr::group_by(Country, Category) |>
  dplyr::summarise(share = sum(Percentage, na.rm = TRUE), .groups = "drop") |>
  dplyr::group_by(Country) |>
  dplyr::slice_max(order_by = share, n = 1, with_ties = FALSE) |>
  dplyr::transmute(
    Country = as.character(Country),
    dominant_sector = as.character(Category),
    dominant_sector_share = share
  )

analysis_summary <- dplyr::tibble(Country = analysis_country_levels) |>
  dplyr::left_join(fuel_top, by = "Country") |>
  dplyr::left_join(sector_top, by = "Country") |>
  dplyr::mutate(
    dominant_fuel = dplyr::coalesce(dominant_fuel, "N/A"),
    dominant_sector = dplyr::coalesce(dominant_sector, "N/A"),
    dominant_fuel_share = dplyr::coalesce(dominant_fuel_share, 0),
    dominant_sector_share = dplyr::coalesce(dominant_sector_share, 0),
    vulnerability_index = (dominant_fuel_share * dominant_sector_share) / 100
  )

analysis_summary_table <- analysis_summary |>
  dplyr::arrange(desc(vulnerability_index)) |>
  dplyr::mutate(Country = factor(Country, levels = rev(analysis_country_levels))) |>
  dplyr::transmute(
    Country = as.character(Country),
    `Dominant Fuel` = dominant_fuel,
    `Dominant Sector` = dominant_sector,
    `Vulnerability` = round(vulnerability_index, 1)
  )
```

```{r}
#| label: final-vulnerability-plot
#| eval: true
#| echo: false
#| fig-width: 12
#| fig-height: 10
#| out-width: "100%"
#| dev: png
#| message: false
#| warning: false

plot_data <- analysis_summary |>
  dplyr::arrange(desc(vulnerability_index), Country) |>
  dplyr::mutate(
    Country = factor(Country, levels = rev(unique(Country))),
    dominant_sector = dplyr::if_else(
      dominant_fuel == "N/A" | dominant_sector == "N/A",
      "N/A",
      dominant_sector
    ),
    dominant_fuel = dplyr::if_else(
      dominant_fuel == "N/A" | dominant_sector == "N/A",
      "N/A",
      dominant_fuel
    )
  )

ggplot2::ggplot(
  plot_data,
  ggplot2::aes(
    x = Country,
    y = vulnerability_index,
    fill = dominant_fuel,
    color = dominant_sector
  )
) +
  ggplot2::geom_col(width = 0.7, linewidth = 0.45) +
  ggplot2::geom_text(
    ggplot2::aes(label = dominant_sector, color = dominant_sector),
    vjust = -0.45,
    size = data_chart_geom_text_size,
    show.legend = FALSE
  ) +
  ggplot2::labs(
    title = "Dominant-Pair Vulnerability by Country",
    subtitle = "Derived from dominant fuel share × dominant sector share",
    x = "Country",
    y = "Vulnerability",
    fill = "Dominant Fuel",
    color = "Most Affected Sector"
  ) +
  ggplot2::scale_fill_brewer(palette = "Set2") +
  ggplot2::scale_color_brewer(palette = "Dark2", na.translate = FALSE) +
  data_chart_theme(
    legend_position = "right",
    axis_x_angle = 35,
    axis_x_hjust = 1
  ) + ggplot2::theme(
    plot.margin = ggplot2::margin(6, 5, 8, 5)
  )
```

:::

:::


## Thank You {.thank-you}

<div class="thank-you-center">
  <h1>Thank You</h1>
</div>




